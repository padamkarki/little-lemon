import logo from "../images/Logo.svg";

const Header = () => {
  return (
    <>
      <header>
        <img src={logo} alt="logo" width={200} />
      </header>
    </>
  );
};

export default Header;
